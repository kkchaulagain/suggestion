# Server
PORT=3000

# MongoDB (omit to use in-memory DB in tests)
MONGODB_URI=mongodb://localhost:27017/suggestion

# JWT signing (set in production)
JWT_SECRET=your-secret-key-change-in-production

# Optional: base URL for feedback form links in QR codes (e.g. https://yourapp.com/forms)
# FRONTEND_FORM_BASE_URL=https://frontend.example.com/forms
