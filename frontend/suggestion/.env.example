# API base URL (no trailing slash). Defaults to http://localhost:3001 if unset.
# For production build (e.g. Fly), set when building: VITE_API_URL=https://suggestion.fly.dev
VITE_API_URL=http://localhost:3001
